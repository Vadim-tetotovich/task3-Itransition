import Util from './util';

let mapList = null;

export default (getOutEnumerator) => {
    if (mapList) {
        return mapList;
    }
    if (Util.isFunction(getOutEnumerator)) {
        mapList = getOutEnumerator();
    } else {
        return null;
    }
};
